import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  header: {
    justifyContent: "center",
    left: 0,
    right: 0,
    height: 60,
  },

  textStyle: {
    textAlign: "center",
    color: "#000",
    fontSize: 16,
    padding: 20,
  },

  headerText: {
    color: "#fff",
    fontSize: 18,
  },
});
